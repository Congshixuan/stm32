# STM32 HAL 库 多通道同步呼吸灯模块化实现

**整体实现思路**

1.**功能需求**：

TIM2_CH2/CH3/CH4 三路 LED 同步 PWM 呼吸，亮度 0~1000 往复渐变，封装独立 BSP 文件，主函数仅调用接口。

2.**核心原理**:

- 定时器 PWM 输出，修改捕获比较寄存器值调整占空比；
- 变量`pwm_val`存储当前比较值，`dir`控制增减方向；
- 到达边界值自动反转变化方向，搭配延时控制呼吸速度；
- 代码解耦：功能封装`breath_led.c`，对外接口声明`breath_led.h`，主文件引入头文件调用。

